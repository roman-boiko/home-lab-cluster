---
extends: default

ignore: |
  AGENTS.md
  CLAUDE.MD
  docs/**
  kubeconfig/**

rules:
  document-start: disable
  empty-lines: disable
  line-length:
    max: 300
  truthy:
    allowed-values: ["true", "false", "on"]
